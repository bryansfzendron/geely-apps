<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Testa se conseguimos enxergar os apps ja instalados (Android 11+ restringe isso).
         A loja precisa disso para saber o que ja esta instalado / tem atualizacao. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Nao exigimos nenhum hardware: se a unidade nao tiver algo, o app ainda instala
         e nos conta o que falta, em vez de ser recusado na instalacao. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.type.automotive" android:required="false" />

    <application
        android:label="Diagnostico Geely"
        android:icon="@android:drawable/ic_dialog_info"
        android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
